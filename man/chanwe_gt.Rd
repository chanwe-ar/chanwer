% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt-theme.R
\name{chanwe_gt}
\alias{chanwe_gt}
\title{Chanwe Table via gt (HTML)}
\usage{
chanwe_gt(
  data,
  title = NULL,
  subtitle = NULL,
  eyebrow = NULL,
  caption = NULL,
  stub = NULL,
  signed = NULL,
  smaller_is_better = FALSE,
  density = c("spacious", "compact"),
  bg = "white",
  id = NULL
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{title}{Table title (Archivo, 20px).}

\item{subtitle}{Subtitle line rendered below the title.}

\item{eyebrow}{Small mono-caps label with an orange rule prefix, rendered
above the title.}

\item{caption}{Source note at the bottom, prefixed with \verb{//}.}

\item{stub}{Name of the stub/row-identifier column (passed to
\code{gt::gt(rowname_col = )}). Rendered left-aligned in ink with a thin
vertical rule on its right.}

\item{signed}{Optional character vector of column names whose values are
coloured by valence with the canonical signed tokens (positive green,
negative vermillion, zero/NA neutral). Formatting is untouched -- pair it
with \code{gt::fmt()} for \verb{+x.x\%} rendering.}

\item{smaller_is_better}{Logical. Flip the valence mapping for \code{signed}
columns where a negative delta is good (costs, churn). Default \code{FALSE}.}

\item{density}{\code{"spacious"} (default) or \code{"compact"} row padding.}

\item{bg}{Table background. Named shorthand accepted by \code{\link[=theme_chanwe]{theme_chanwe()}}
(\code{"white"}, \code{"white-ivory"}, \code{"metallic"}, \code{"gray"}, \code{"beige"},
\code{"transparent"}) or any hex string. Default \code{"white"}.}

\item{id}{Optional HTML id for the table. A random id is generated when
\code{NULL} so the scoped CSS never leaks between tables.}
}
\value{
A \code{gt_tbl} object.
}
\description{
The HTML counterpart of \code{\link[=chanwe_kbl]{chanwe_kbl()}}: a \code{gt} table styled with the
Chanwe header grammar -- mono-caps eyebrow with an orange rule prefix,
Archivo title, Satoshi subtitle, mono-caps column labels, JetBrains Mono
tabular figures right-aligned, a hairline ink rule above the table, a
thin neutral rule under the column labels and to the right of the stub,
flat rows (an n100 tint on hover, matching \code{\link[=chanwe_reactable]{chanwe_reactable()}}, no
zebra striping), and a \verb{//}-prefixed source note.
}
\details{
The function returns a regular \code{gt_tbl}, so every \code{gt} verb
(\code{gt::fmt_number()}, \code{gt::cols_label()}, \code{gt::tab_style()}, ...) can be
piped after it.

Fonts: the table uses the brand faces when they are available on the
page. Load them with \code{\link[=chanwe_reporting_css]{chanwe_reporting_css()}} in Quarto HTML documents.
}
\examples{
\dontshow{if (requireNamespace("gt", quietly = TRUE)) withAutoprint(\{ # examplesIf}
df <- data.frame(
  metric = c("Revenue", "EBITDA", "Net income"),
  value = c(45.9, 12.3, 8.1),
  delta = c(12.4, -3.1, 5.8)
)
tbl <- chanwe_gt(
  df,
  title = "Quarterly deltas",
  eyebrow = "TABLE · P&L",
  subtitle = "USD MM and change vs. plan.",
  caption = "Source · Internal ledger.",
  stub = "metric",
  signed = "delta"
) |>
  gt::fmt(columns = "delta", fns = function(x) sprintf("\%+.1f\%\%", x))
\dontshow{\}) # examplesIf}
}
