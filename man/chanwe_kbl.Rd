% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kbl-theme.R
\name{chanwe_kbl}
\alias{chanwe_kbl}
\title{ChanWe Table via Native Typst Output}
\usage{
chanwe_kbl(
  data,
  title = NULL,
  subtitle = NULL,
  eyebrow = NULL,
  caption = NULL,
  full_width = TRUE,
  col_widths = NULL,
  col_aligns = NULL,
  col_labels = NULL,
  stub = NULL,
  density = c("spacious", "compact"),
  row_padding = NULL,
  title_size = "13pt",
  eyebrow_size = "4pt",
  subtitle_size = "8pt",
  body_size = "7pt",
  header_size = "5.5pt",
  note_size = "5.5pt",
  col_label_top = 0,
  footer_top = 0,
  bg = "white-ivory",
  top_border = TRUE,
  header_rule = TRUE,
  padding = 12.5,
  fmt = list(),
  col_colors = list(),
  n_total = 0,
  total_fill = TRUE,
  vlines = NULL,
  highlight_cols = NULL,
  highlight_color = "#F5F1EB"
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{title}{Table title.}

\item{subtitle}{Subtitle line rendered below the title.}

\item{eyebrow}{Small mono-caps label with an orange horizontal rule prefix.}

\item{caption}{Source note at the bottom in JetBrains Mono.}

\item{full_width}{Logical. If \code{TRUE}, columns share the full available
width equally (\code{"1fr"} each). Overridden by \code{col_widths}.}

\item{col_widths}{Character vector of Typst column width specs, e.g.
\code{c("1fr", "auto", "20\%")}. Defaults to \code{"auto"} for every column.}

\item{col_aligns}{Per-column alignment: \code{"left"}, \code{"right"}, or
\code{"center"}. Single value is recycled. Defaults to auto-detect (right
for numeric, left otherwise).}

\item{col_labels}{Named character vector to override displayed column names.}

\item{stub}{Name of the stub/row-identifier column. Displayed left-aligned
in muted ink; excluded from auto numeric-alignment detection.}

\item{density}{\code{"spacious"} (default) or \code{"compact"}.}

\item{row_padding}{Typst size string overriding the vertical cell inset for
data rows and column labels (e.g. \code{"6pt"}). Defaults to \code{"10pt"}
(spacious) or \code{"5pt"} (compact).}

\item{title_size}{Typst size string for the title. Default \code{"13pt"};
pass \code{NULL} for the density-based default (\code{"19pt"} spacious /
\code{"16pt"} compact).}

\item{eyebrow_size}{Typst size string for the eyebrow. Default \code{"4pt"}.}

\item{subtitle_size}{Typst size string for the subtitle. Default \code{"8pt"};
\code{NULL} for the density-based default (\code{"10pt"} / \code{"9pt"}).}

\item{body_size}{Typst size string for data cell text. Default \code{"7pt"};
\code{NULL} for the density-based default (\code{"10pt"} / \code{"7pt"}).}

\item{header_size}{Typst size string for column label text. Default
\code{"5.5pt"}; \code{NULL} for the density-based default
(\code{"8pt"} / \code{"7pt"}).}

\item{note_size}{Typst size string for the footer note. Default
\code{"5.5pt"}; \code{NULL} for the density-based default
(\code{"8pt"} / \code{"7pt"}).}

\item{col_label_top}{Extra vertical space in pt above column label text
(between the separator line and the labels). Default \code{0}.}

\item{footer_top}{Extra vertical space in pt above the footer note text.
Default \code{0}.}

\item{bg}{Table background colour. Named shorthand: \code{"white-ivory"}
(default, \code{#FAF9F7}), \code{"white"}, \code{"beige"} (\code{#F5F1EB}),
\code{"gray"} (\code{#EDF0F1}), \code{"metallic"} (\code{#F7F7F7}).
Or any raw Typst color expression (e.g. \code{"rgb(\\\"#EEF0F2\\\")"}). Pass \code{NULL} for transparent.}

\item{top_border}{Logical. Draw a thin ink line above the title cell.
Default \code{TRUE}.}

\item{header_rule}{Logical. Draw the heavier rule between the title block
and the column labels. Default \code{TRUE}.}

\item{padding}{Uniform outer margin in pts applied around the entire table
block (equivalent to \code{plot_padding} in \code{theme_chanwe()}).
Default \code{12.5}.}

\item{fmt}{Named list of formatting functions, keyed by column name.
Each function receives the column vector and must return a character vector.}

\item{col_colors}{Named list of functions, keyed by column name. Each
function receives the column's \emph{original} values — before \code{fmt}
is applied — and must return a character vector of raw Typst color
expressions (e.g. \code{"rgb(\\\"#CC1914\\\")"}, not escaped) used as the
text fill for each cell. This is deliberate: color logic can branch on
raw numbers (e.g. the sign of a delta) even when \code{fmt} renders the
same column as text. For the standard positive/negative treatment use
\code{\link{chanwe_col_signed}()} — see the examples.}

\item{n_total}{Number of trailing rows to treat as total rows. Total rows
get a heavier rule above them, no row dividers, and (optionally) a
highlight fill. Default \code{0} (no total rows).}

\item{total_fill}{Logical. Fill total rows with a light gray background.
Only used when \code{n_total > 0}. Default \code{TRUE}.}

\item{vlines}{Optional integer vector of column indices at which to draw
vertical divider lines (Typst \code{table.vline} x positions).}

\item{highlight_cols}{Optional integer vector of column indices whose header
and data cells get a highlight background fill.}

\item{highlight_color}{Hex color for \code{highlight_cols} fill.
Default \code{"#F5F1EB"}.}
}
\value{
A \code{\link[knitr]{asis_output}} containing a raw \code{{=typst}} block.
Works in Quarto documents rendered with \code{format: chanwe-report-typst}.
}
\description{
Generates a styled Typst table directly from a data frame, bypassing the
HTML→Pandoc→Typst pipeline. Archivo title, Satoshi body, JetBrains Mono
column headers, thin ink divider lines. No CSS translation losses.
}
\examples{
\dontshow{if (requireNamespace("knitr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
df <- data.frame(
  metric = c("Revenue", "EBITDA", "Net income"),
  delta = c(12.4, -3.1, 5.8)
)

## Signed-deltas pattern: fmt renders the delta as text, while the
## col_colors function still receives the raw numbers and colors by sign
## using the canonical ChanWe signed tokens.
chanwe_kbl(
  df,
  title = "Quarterly deltas",
  fmt = list(delta = function(x) sprintf("\%+.1f\%\%", x)),
  col_colors = list(delta = chanwe_col_signed())
)
\dontshow{\}) # examplesIf}
}
