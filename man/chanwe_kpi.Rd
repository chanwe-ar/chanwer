% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2-theme.R
\name{chanwe_kpi}
\alias{chanwe_kpi}
\title{ChanWe KPI Panel Encoder}
\usage{
chanwe_kpi(
  num,
  label = "",
  period = "",
  mtc1_num = NULL,
  mtc1_label = NULL,
  mtc1_direction = "+",
  mtc2_num = NULL,
  mtc2_label = NULL,
  mtc2_direction = "+",
  mtc3_num = NULL,
  mtc3_label = NULL,
  mtc3_direction = "+"
)
}
\arguments{
\item{num}{Hero value shown in large italic type. \strong{Must be a pre-formatted
string} — formatting (decimal separator, rounding) is your responsibility,
e.g. \code{"45,91"} not \code{45.91}.}

\item{label}{Unit or currency label shown in small caps above the date,
e.g. \code{"USD MM"}. Pass \code{""} to omit.}

\item{period}{Date or period string shown below the label,
e.g. \code{"05·MAY·2026"}. Pass \code{""} to omit.}

\item{mtc1_num, mtc2_num, mtc3_num}{Pre-formatted value for each period
metric, e.g. \code{"0,19\%"}. Set both \code{mtcN_num} \strong{and} \code{mtcN_label} to
non-\code{NULL} to display a metric; set either to \code{NULL} to omit it entirely.}

\item{mtc1_label, mtc2_label, mtc3_label}{Short header rendered above the
metric value, e.g. \code{"WoW"}, \code{"MoM"}, \code{"YoY"}.}

\item{mtc1_direction, mtc2_direction, mtc3_direction}{\code{"+"} → ▲ in green; \code{"-"} → ▼ in red; any other value → no arrow,
neutral ink. Default \code{"+"}.}
}
\value{
An opaque encoded string. Pass it to the \code{kpi} argument of
\code{\link[=chanwe_subtitle]{chanwe_subtitle()}}; do not use it directly in \code{labs()}.
}
\description{
Builds the KPI scoreboard that sits between the subtitle and the chart.
Always wrap the result in \code{\link[=chanwe_subtitle]{chanwe_subtitle()}} and pass that to
\code{labs(subtitle = ...)} — never use the output of \code{chanwe_kpi()} directly.
}
\details{
\subsection{Panel layout}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{── subtitle text ────────────────────────────────────────────

  45,91          WOW    MOM    YOY
  USD MM       ▲ 0,19\% ▲ 3,29\% ▲ 17,78\%
  05·MAY·2026

── chart ────────────────────────────────────────────────────
}\if{html}{\out{</div>}}
\itemize{
\item \strong{Left block} — hero value (\code{num}), unit label (\code{label}), date (\code{period})
\item \strong{Right block} — up to 3 period metrics, right-anchored, each with a
short label row (e.g. WOW) and a value row with ▲/▼ direction indicator
}
}

\subsection{Call pattern}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{labs(
  subtitle = chanwe_subtitle(
    "Your subtitle text",
    kpi = chanwe_kpi(num = ..., label = ..., period = ..., ...)
  )
)
}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
}
}
\examples{
## Minimal — hero value only, no period metrics:
chanwe_kpi(num = "45,91", label = "USD MM", period = "05·MAY·2026")

## With all three period metrics:
chanwe_kpi(
  num = "45,91", label = "USD MM", period = "05·MAY·2026",
  mtc1_num = "0,19\%",  mtc1_label = "WoW", mtc1_direction = "+",
  mtc2_num = "3,29\%",  mtc2_label = "MoM", mtc2_direction = "+",
  mtc3_num = "17,78\%", mtc3_label = "YoY", mtc3_direction = "-"
)

## Full plot — the typical usage:
if (requireNamespace("ggplot2", quietly = TRUE)) {
  ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
    ggplot2::geom_line() +
    ggplot2::labs(
      title    = chanwe_title("Fleet overview", eyebrow = "MOTOR TREND"),
      subtitle = chanwe_subtitle(
        "Highway mpg vs vehicle weight",
        kpi = chanwe_kpi(
          num    = "21,0",   label  = "MPG",  period = "1974",
          mtc1_num = "2,1\%", mtc1_label = "WoW", mtc1_direction = "+",
          mtc2_num = "0,5\%", mtc2_label = "MoM", mtc2_direction = "-",
          mtc3_num = "4,3\%", mtc3_label = "YoY", mtc3_direction = "+"
        )
      ),
      caption = chanwe_caption("Source: mtcars")
    ) +
    theme_chanwe()
}
}
\seealso{
\code{\link[=chanwe_subtitle]{chanwe_subtitle()}}, \code{\link[=chanwe_title]{chanwe_title()}}, \code{\link[=theme_chanwe]{theme_chanwe()}}
}
