% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt-theme.R
\name{chanwe_gt_header}
\alias{chanwe_gt_header}
\title{ChanWe GT Header Helper}
\usage{
chanwe_gt_header(data, title, subtitle = NULL, eyebrow = NULL, caption = NULL)
}
\arguments{
\item{data}{A gt table object created with \code{\link[gt:gt]{gt::gt()}}.}

\item{title}{Main title string. Plain text; HTML is added automatically when
\code{eyebrow} is not \code{NULL}.}

\item{subtitle}{Subtitle string rendered below the title in light-weight
Archivo. \code{NULL} (default) omits the subtitle row entirely.}

\item{eyebrow}{Short section label shown above the title in orange
JetBrains Mono mono-caps, preceded by an orange \verb{──} rule
(e.g. \code{"SECTION · FLEET"}). Automatically upper-cased. \code{NULL} (default)
omits the eyebrow.}

\item{caption}{Source note string appended below the table body via
\code{\link[gt:tab_source_note]{gt::tab_source_note()}}. Styled in small JetBrains Mono by
\code{\link[=gt_theme_chanwe]{gt_theme_chanwe()}}. \code{NULL} (default) omits the source note.}
}
\value{
The input gt table with the header and optional source note applied.
Returns a gt object that can be further piped into \code{\link[=gt_theme_chanwe]{gt_theme_chanwe()}} or
other gt functions.
}
\description{
Applies \code{\link[gt:tab_header]{gt::tab_header()}} and optionally \code{\link[gt:tab_source_note]{gt::tab_source_note()}} in a
single pipe step, with built-in support for the ChanWe eyebrow treatment.
When \code{eyebrow} is provided the title is automatically wrapped in
\code{\link[gt:html]{gt::html()}} with \code{\link[=chanwe_gt_eyebrow]{chanwe_gt_eyebrow()}} prepended — no manual
\code{paste0(chanwe_gt_eyebrow(...), title)} needed.
}
\details{
\subsection{Anatomy of a ChanWe gt header}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{── SECTION · LABEL         ← eyebrow  (orange, JetBrains Mono, 7.5 pt)
Title text                 ← title    (Archivo Bold 700, ~40 px)
Subtitle text              ← subtitle (Archivo ExtraLight 200, ~16 px)
─────────────────────────────────────────────────────────
 col A   col B   col C     ← table body
 ...
─────────────────────────────────────────────────────────
Source · caption text      ← caption  (JetBrains Mono, ~10 px)
}\if{html}{\out{</div>}}
}

\subsection{Parameter combinations}{\tabular{llll}{
   \code{eyebrow} \tab \code{subtitle} \tab \code{caption} \tab Result \cr
   \code{NULL} \tab \code{NULL} \tab \code{NULL} \tab Title only \cr
   set \tab \code{NULL} \tab \code{NULL} \tab Eyebrow + title \cr
   \code{NULL} \tab set \tab \code{NULL} \tab Title + subtitle \cr
   set \tab set \tab \code{NULL} \tab Eyebrow + title + subtitle \cr
   set \tab set \tab set \tab Full header — eyebrow, title, subtitle, caption \cr
}

}
}
\examples{
if (requireNamespace("gt", quietly = TRUE)) {

  ## Title only — minimal header
  gt::gt(head(mtcars, 5)) |>
    chanwe_gt_header(title = "Motor Trend Cars") |>
    gt_theme_chanwe()

  ## Title + subtitle, no eyebrow
  gt::gt(head(mtcars, 5)) |>
    chanwe_gt_header(
      title    = "Motor Trend Cars",
      subtitle = "Top 5 rows · mtcars dataset"
    ) |>
    gt_theme_chanwe()

  ## Title + eyebrow, no subtitle
  gt::gt(head(mtcars, 5)) |>
    chanwe_gt_header(
      title   = "Fuel Efficiency Overview",
      eyebrow = "TLDR;"
    ) |>
    gt_theme_chanwe()

  ## Full header — eyebrow, title, subtitle, caption
  mt <- tibble::as_tibble(mtcars, rownames = "model")
  gt::gt(head(mt, 6), rowname_col = "model") |>
    chanwe_gt_header(
      title    = "Operational Snapshot",
      subtitle = "Fleet overview · mtcars sample · Q1 2026",
      eyebrow  = "SECTION · OPERATIONAL",
      caption  = "Source · Motor Trend, 1974 · mtcars dataset"
    ) |>
    gt_theme_chanwe(bg_color = "beige")

  ## Compact variant with gray background
  gt::gt(head(mtcars, 10)) |>
    chanwe_gt_header(
      title   = "Fleet Metrics",
      eyebrow = "DASHBOARD · Q1",
      caption = "Source · Motor Trend, 1974"
    ) |>
    gt_theme_chanwe(variant = "compact", bg_color = "gray")
}
}
\seealso{
\code{\link[=gt_theme_chanwe]{gt_theme_chanwe()}}, \code{\link[=chanwe_gt_eyebrow]{chanwe_gt_eyebrow()}}
}
