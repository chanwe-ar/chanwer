% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2-theme.R
\name{theme_chanwe}
\alias{theme_chanwe}
\title{ChanWe ggplot2 Theme}
\usage{
theme_chanwe(
  base_text_size = 6.75,
  base_family = "Satoshi",
  base_lineheight = 1.6,
  legend_position = "bottom",
  bg_color = "metallic",
  plot_padding = 10,
  plot_borders = "none",
  has_subtitle = TRUE,
  compact_title = TRUE,
  header_line = TRUE
)
}
\arguments{
\item{base_text_size}{Base text size in points. Default \code{6.5}. Title scales at ×1.50, subtitle ×0.90, eyebrow ×0.55.}

\item{base_family}{Base font family for body text. Default \code{"Satoshi"}.}

\item{base_lineheight}{Base line-height multiplier. Default \code{1.60}.}

\item{legend_position}{Legend position string passed to
\code{theme(legend.position = )}. Default \code{"bottom"}.}

\item{bg_color}{Background color for the plot surface. Accepts a hex string
or one of \code{"metallic"} (default), \code{"white-ivory"}, \code{"white"}, \code{"gray"},
\code{"beige"}, or \code{"transparent"}.}

\item{plot_padding}{Uniform outer margin in pts applied to all four sides of
the plot (title, caption, and panel included). Default \code{10}.
Pass a single number, e.g. \code{plot_padding = 18}.}

\item{plot_borders}{Controls decorative border lines on the plot frame.
\code{"none"} (default) draws no borders. \code{"top"} adds a thin ink line above
the title. \code{"bottom"} adds one below the caption. \code{"top_bottom"} adds
both. \code{"complete"} adds all four sides. Pass \code{TRUE} as shorthand for
\code{"top_bottom"}.}

\item{has_subtitle}{Set to \code{FALSE} when the plot has no subtitle. Reduces
the title's bottom margin and draws a separator line below the title text
(matching the line that the subtitle grob normally draws).}

\item{compact_title}{When \code{TRUE} (default) reduces the header block in three
ways: top padding above the eyebrow 8 → 4 pt, gap between subtitle text
and separator line 14 → 6 pt, and bottom padding below the separator line
20 → 12 pt. Set to \code{FALSE} for the spacious layout.}

\item{header_line}{When \code{TRUE} (default) draws the thin separator line that
visually closes the header area: the subtitle grob draws it between the
subtitle text and the chart, and when \code{has_subtitle = FALSE} the title grob
draws it below the title text. Set to \code{FALSE} to suppress the line entirely
(e.g. for minimal or embedded layouts).}
}
\value{
A ggplot2 theme object. Add to any ggplot with \code{+ theme_chanwe()}.
}
\description{
A clean editorial ggplot2 theme with ChanWe typography, neutral surfaces,
orange accents, and minimal chrome. Pairs with \code{\link[=chanwe_title]{chanwe_title()}},
\code{\link[=chanwe_subtitle]{chanwe_subtitle()}}, and \code{\link[=chanwe_caption]{chanwe_caption()}} for the full ChanWe title
treatment, and with \code{\link[=scale_color_chanwe_d]{scale_color_chanwe_d()}} / \code{\link[=scale_fill_chanwe_d]{scale_fill_chanwe_d()}} for
brand-consistent color palettes.
}
\details{
\subsection{Typography}{\tabular{lll}{
   Element \tab Font \tab Weight \cr
   Title (with eyebrow) \tab Archivo \tab 400 \cr
   Subtitle \tab Satoshi \tab 400 \cr
   Axis text \tab Satoshi \tab 400 \cr
   Axis titles \tab JetBrains Mono Thin \tab 100 \cr
   Facet strip labels \tab JetBrains Mono Thin \tab 100 \cr
   Legend text / title \tab JetBrains Mono \tab 400 \cr
   Caption \tab JetBrains Mono \tab 400 \cr
}


Call \code{\link[=chanwe_load_fonts]{chanwe_load_fonts()}} once per session to register the custom font
families. \code{theme_chanwe()} calls it automatically.
}

\subsection{Background variants}{\tabular{llll}{
   Name \tab Hex \tab Major grid \tab Minor grid \cr
   \code{"metallic"} \tab \verb{#F7F7F7} \tab \verb{#E4E4E4} \tab \verb{#EFEFEF} \cr
   \code{"white-ivory"} \tab \verb{#FAF9F7} \tab \verb{#ECEAE6} \tab \verb{#F1F0ED} \cr
   \code{"white"} \tab \verb{#FFFFFF} \tab \verb{#E8E8E8} \tab \verb{#EEEEEE} \cr
   \code{"gray"} \tab \verb{#EDF0F1} \tab \verb{#D4D9DB} \tab \verb{#E3E7E9} \cr
   \code{"beige"} \tab \verb{#F5F1EB} \tab \verb{#D8D1C7} \tab \verb{#E3DDD5} \cr
}

}
}
\section{Header layout modes}{


The title + subtitle area is built from custom grobs rather than plain
\code{element_text}, which allows the three-line structure (eyebrow / title /
subtitle) and the KPI scoreboard to be positioned with pixel-level control.
The \code{theme_chanwe()} params drive the spacing fields of those grobs:

\describe{
\item{Normal (title + subtitle)}{
Use \code{labs(title = chanwe_title(...), subtitle = chanwe_subtitle(...))}.
\code{compact_title} controls three paddings simultaneously (see param docs).
}
\item{KPI scoreboard}{
Wrap with \code{chanwe_subtitle("text", kpi = chanwe_kpi(...))} or omit the
text entirely with \code{chanwe_subtitle("", kpi = chanwe_kpi(...))}.
When subtitle text is empty, the text row and its separator line are
skipped and the KPI panel starts with just 2 pt of top padding.
}
\item{No subtitle (\code{has_subtitle = FALSE})}{
Pass \code{has_subtitle = FALSE} to \code{theme_chanwe()}. The title grob draws
its own 0.4 pt separator line 15 pt above the chart area (same visual
weight as the subtitle separator). \code{compact_title} still controls the
top padding above the eyebrow.
}
\item{No separator line (\code{header_line = FALSE})}{
Pass \code{header_line = FALSE} to suppress the thin ink line that closes the
header area. Works in both subtitle and no-subtitle modes. Useful for
minimal or embedded layouts where the line adds visual noise.
}
}
}

\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {

  ## Basic scatter — white background, bottom legend
  ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg, color = factor(cyl))) +
    ggplot2::geom_point(size = 3) +
    scale_color_chanwe_d() +
    ggplot2::labs(
      title = chanwe_title("Fuel efficiency by weight"),
      subtitle = "Highway mpg vs vehicle weight",
      caption = chanwe_caption("Source: Motor Trend, 1974")
    ) +
    theme_chanwe()

  ## With eyebrow, beige background, no legend
  ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg, color = factor(cyl))) +
    ggplot2::geom_point(size = 3) +
    scale_color_chanwe_d() +
    ggplot2::labs(
      title = chanwe_title("Fuel efficiency", eyebrow = "SECTION · FLEET"),
      subtitle = "Highway mpg vs vehicle weight",
      caption = chanwe_caption("Source: Motor Trend, 1974")
    ) +
    theme_chanwe(bg_color = "beige", legend_position = "none")

  ## Bar chart — gray background, fill scale
  avg_mpg <- aggregate(mpg ~ cyl, data = mtcars, FUN = mean)
  ggplot2::ggplot(avg_mpg, ggplot2::aes(factor(cyl), mpg, fill = factor(cyl))) +
    ggplot2::geom_col(width = 0.6) +
    scale_fill_chanwe_d() +
    ggplot2::labs(
      title = chanwe_title("Average MPG by cylinder count"),
      caption = chanwe_caption("Source: mtcars")
    ) +
    theme_chanwe(bg_color = "gray", legend_position = "none")

  ## Faceted line chart
  ggplot2::ggplot(ggplot2::economics_long,
    ggplot2::aes(date, value01, color = variable)) +
    ggplot2::geom_line(linewidth = 0.6) +
    ggplot2::facet_wrap(~variable, scales = "free_y", ncol = 2) +
    scale_color_chanwe_d() +
    ggplot2::labs(
      title = chanwe_title("US economic indicators", eyebrow = "MACRO"),
      caption = chanwe_caption("Source: ggplot2::economics_long")
    ) +
    theme_chanwe(legend_position = "none")
}
}
