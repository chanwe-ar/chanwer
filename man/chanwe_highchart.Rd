% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/highchart-theme.R
\name{chanwe_highchart}
\alias{chanwe_highchart}
\title{Chanwe Layout for highcharter Charts (HTML)}
\usage{
chanwe_highchart(
  hc,
  title = NULL,
  subtitle = NULL,
  eyebrow = NULL,
  caption = NULL,
  legend = FALSE,
  height = 440,
  bg = "white"
)
}
\arguments{
\item{hc}{A \code{highcharter} object (from \code{highcharter::highchart()}).}

\item{title, subtitle, eyebrow}{Header block rendered top-left, stacked as
one \code{useHTML} chart title.}

\item{caption}{Source note rendered bottom-left with a \verb{//} prefix.}

\item{legend}{Logical. Show the legend (vertical, top-right)? Default
\code{FALSE}.}

\item{height}{Widget height in pixels, passed to
\code{highcharter::hc_size()} -- needed because \code{hc_chart(height = )} alone
only sets Highcharts' internal chart option; the htmlwidget itself
stays in "fill" sizing mode and renders at zero height outside a
dashboard layout until \code{hc_size()} sets the widget's own height field.}

\item{bg}{Chart and plot background. Named shorthand accepted by
\code{\link[=theme_chanwe]{theme_chanwe()}} or any hex string. Default \code{"white"}.}
}
\value{
The modified \code{highcharter} object.
}
\description{
Applies the Chanwe header grammar and chart chrome to an existing
\code{highcharter} object: eyebrow / title / subtitle block anchored top-left
in Archivo, Satoshi body font, JetBrains Mono axis ticks, no axis lines or
ticks, hairline y-grid, an ink tooltip in mono, no exporting menu or
credits, and an optional \verb{//}-prefixed caption.
}
\details{
Discrete colours are not changed by this function -- the chart palette
(\code{chanwe_palette("chart")}) is applied as the widget's series colours, so
series added without an explicit \code{color} cycle through the brand palette.
}
\examples{
\dontshow{if (requireNamespace("highcharter", quietly = TRUE)) withAutoprint(\{ # examplesIf}
hc <- highcharter::highchart() |>
  highcharter::hc_xAxis(type = "datetime") |>
  highcharter::hc_add_series(
    data = highcharter::list_parse2(data.frame(x = 1:10, y = rnorm(10))),
    type = "line", name = "Series A"
  )
hc <- chanwe_highchart(
  hc,
  title = "Fleet overview",
  eyebrow = "SECTION · EFFICIENCY",
  subtitle = "Simulated values over time.",
  caption = "Source · Simulated data."
)
\dontshow{\}) # examplesIf}
}
