% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotly-theme.R
\name{chanwe_plotly}
\alias{chanwe_plotly}
\title{Chanwe Layout for plotly Charts (HTML)}
\usage{
chanwe_plotly(
  p,
  title = NULL,
  subtitle = NULL,
  eyebrow = NULL,
  caption = NULL,
  legend = FALSE,
  hovermode = "x unified",
  bg = "white"
)
}
\arguments{
\item{p}{A \code{plotly} object.}

\item{title, subtitle, eyebrow}{Header block rendered top-left. Plain text;
plotly's limited pseudo-HTML is applied internally.}

\item{caption}{Source note rendered bottom-left with a \verb{//} prefix.}

\item{legend}{Logical. Show the legend (horizontal, bottom-right)?
Default \code{FALSE} -- prefer direct labels via \code{plotly::add_annotations()}.}

\item{hovermode}{Passed to \code{plotly::layout()}. Default \code{"x unified"}.}

\item{bg}{Paper and plot background. Named shorthand accepted by
\code{\link[=theme_chanwe]{theme_chanwe()}} or any hex string. Default \code{"white"}.}
}
\value{
The modified \code{plotly} object.
}
\description{
Applies the Chanwe header grammar and chart chrome to an existing
\code{plotly} object: eyebrow / title / subtitle block anchored top-left in
Archivo, Satoshi body font, JetBrains Mono axis ticks, no axis lines or
ticks, hairline y-grid, an ink hover card in mono (\code{hovermode = "x unified"} by default), a thin cursor spike, no modebar, no drag-zoom, and
an optional \verb{//}-prefixed caption.
}
\details{
Discrete colours are not changed by this function -- pass
\code{colors = chanwe_palette("chart")} to \code{plotly::plot_ly()}. The layout
\code{colorway} is set to the chart palette for traces that do not carry an
explicit colour.
}
\examples{
\dontshow{if (requireNamespace("plotly", quietly = TRUE)) withAutoprint(\{ # examplesIf}
p <- plotly::plot_ly(
  mtcars, x = ~wt, y = ~mpg, color = ~factor(cyl),
  colors = unname(chanwe_palette("chart"))[1:3],
  type = "scatter", mode = "markers"
)
p <- chanwe_plotly(
  p,
  title = "Fleet overview",
  eyebrow = "SECTION · EFFICIENCY",
  subtitle = "Weight against fuel economy.",
  caption = "Source · Motor Trend, 1974."
)
\dontshow{\}) # examplesIf}
}
